# ╔══════════════════════════════════════════════════════════════════════════╗
# ║  IVF Cloud Replica — Environment Variables                              ║
# ║                                                                          ║
# ║  Copy this file to .env and fill in every value marked REQUIRED.         ║
# ║  Never commit the filled-in .env to version control.                    ║
# ╚══════════════════════════════════════════════════════════════════════════╝

# ── Primary PostgreSQL server ─────────────────────────────────────────────────
# Public IP address or FQDN of the server running docker-compose.yml
PRIMARY_HOST=203.0.113.10          # <<< REQUIRED — primary server public IP/hostname

# Port exposed on the primary host (see docker-compose.yml → db service)
PRIMARY_PORT=5433

# ── Replication credentials (must match the primary's replicator role) ─────────
REPLICATOR_USER=replicator
REPLICATOR_PASSWORD=CHANGE_ME_strong_password   # <<< REQUIRED

# Replication slot created on the primary (see CloudReplicationService.SetupDbReplication)
REPLICA_SLOT_NAME=cloud_standby_slot

# application_name shown in pg_stat_replication on primary
REPLICA_APPLICATION=ivf-cloud-replica

# SSL mode for the replication connection
# Options: require | verify-ca | verify-full | disable (not recommended)
PGSSLMODE=require

# ── PostgreSQL superuser ───────────────────────────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_pg_password   # <<< REQUIRED — same as primary
POSTGRES_DB=ivf_db

# Port on THIS host where PostgreSQL is accessible
# Use 5432 (default) or any free port; register this in CloudReplicationConfig.RemoteDbPort
REPLICA_DB_PORT=5432

# ── MinIO object storage ───────────────────────────────────────────────────────
MINIO_ROOT_USER=minioadmin            # <<< REQUIRED — set in CloudReplicationConfig.RemoteMinioAccessKey
MINIO_ROOT_PASSWORD=CHANGE_ME_minio  # <<< REQUIRED — set in CloudReplicationConfig.RemoteMinioSecretKey

MINIO_SITE_NAME=ivf-replica
MINIO_REGION=us-east-1

# Ports on THIS host
MINIO_API_PORT=9000     # Used by CloudReplicationConfig.RemoteMinioEndpoint (e.g. http://203.0.113.10:9000)
MINIO_CONSOLE_PORT=9001
